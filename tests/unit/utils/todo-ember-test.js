import todoEmber from 'em-todo/utils/todo-ember';
import { module, test } from 'qunit';

module('Unit | Utility | todo-ember', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = todoEmber();
    assert.ok(result);
  });
});
